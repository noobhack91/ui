// react/src/utils/bahmniDateInStrictMode.ts

/**
 * Utility function to format a date in strict mode.
 * This function is a conversion from the AngularJS filter 'bahmniDateInStrictMode'.
 * 
 * @param date - The date to be formatted.
 * @returns The formatted date string.
 */
export function formatDateInStrictMode(date: Date): string {
    // Assuming Bahmni.Common.Util.DateUtil.formatDateInStrictMode is a function that formats the date
    // You need to replace this with the actual implementation or import the function if it exists
    return Bahmni.Common.Util.DateUtil.formatDateInStrictMode(date);
}
