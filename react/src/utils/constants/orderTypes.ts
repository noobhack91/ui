// Define the type for order types
type OrderTypes = {
    lab: string;
    radiology: string;
};

// Define the constant for order types
const orderTypes: OrderTypes = {
    lab: "Lab Order",
    radiology: "Radiology Order"
};

export default orderTypes;
