// Define the type for dosing types
type DosingTypes = {
    uniform: string;
    variable: string;
};

// Create the constant for dosing types
const dosingTypes: DosingTypes = {
    uniform: "uniform",
    variable: "variable"
};

export default dosingTypes;
