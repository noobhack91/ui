// react/src/utils/constants/representation.ts

const representation: string = "custom:(uuid,name,names,conceptClass," +
    "setMembers:(uuid,name,names,conceptClass," +
    "setMembers:(uuid,name,names,conceptClass," +
    "setMembers:(uuid,name,names,conceptClass))))";

export default representation;
